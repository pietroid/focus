name: notifications
description: Mirrors the server's reminder plan into the device's local notification queue.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.12.0
  flutter: ^3.44.0

dependencies:
  api_client:
    path: ../api_client
  equatable: ^2.0.7
  flutter:
    sdk: flutter
  flutter_local_notifications: ^22.3.1
  shared_preferences: ^2.5.5
  timezone: ^0.11.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.5
  very_good_analysis: ^10.3.0
